node_modules/
dist/
*.tsbuildinfo

# Local secrets
.env
.env.local
*.local

# SQLite database and its write-ahead log.
# Scoped deliberately: a bare `data/` would also match
# packages/server/src/data/, excluding the shipped airport dataset.
/data/
packages/server/data/
*.db
*.db-shm
*.db-wal

# Windows shortcuts store an absolute path, so they are machine-specific.
# Recreate them with scripts\create-shortcuts.ps1 instead of committing them.
*.lnk

# Editors
.idea/
.vscode/
.DS_Store

# Logs
*.log
npm-debug.log*
